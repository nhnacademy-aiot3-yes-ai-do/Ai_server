name: Setup test env
description: CI 테스트용 DB/JWT 환경변수 설정
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        # postgreSQL
        echo "DB_HOST=localhost" >> "$GITHUB_ENV"
        echo "DB_PORT=5432" >> "$GITHUB_ENV"
        echo "DB_NAME=testdb" >> "$GITHUB_ENV"
        echo "DB_USERNAME=test" >> "$GITHUB_ENV"
        echo "DB_PASSWORD=test1234" >> "$GITHUB_ENV"
        echo "AI_DB_SCHEMA=public" >> "$GITHUB_ENV"

        # Redis
        echo "REDIS_HOST=localhost" >> "$GITHUB_ENV"
        echo "REDIS_PORT=6379" >> "$GITHUB_ENV"
        echo "REDIS_PASSWORD=" >> "$GITHUB_ENV"

        # RabbitMQ
        echo "RABBITMQ_HOST=localhost" >> "$GITHUB_ENV"
        echo "RABBITMQ_PORT=5672" >> "$GITHUB_ENV"
        echo "RABBITMQ_USERNAME=test" >> "$GITHUB_ENV"
        echo "RABBITMQ_PASSWORD=test1234" >> "$GITHUB_ENV"
        
        # Gemini
        echo "GEMINI_API_KEY=test1234" >> "$GITHUB_ENV"

        # Openfeign
        echo "CULTIVATION_SERVER_URL=http://localhost:8084" >> "$GITHUB_ENV"

        # Vision
        echo "VISION_SERVER_URL=http://vision-test.invalid" >> "$GITHUB_ENV"
        
        # Minio
        echo "IMAGE_DOWNLOAD_ALLOWED_ORIGIN=http://storage.java21.net:9999" >> "$GITHUB_ENV"
